name: Deploy Alpha

on:
  pull_request:
    branches: [main]

  workflow_dispatch:

concurrency:
  group: 'pages-deploy'

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.version.outputs.new_version }}
      main_version: ${{ steps.version.outputs.main_version }}
      alpha_version: ${{ steps.version.outputs.alpha_version }}
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/version-setter
        id: version

  build:
    needs: version
    runs-on: ubuntu-latest
    env:
      main_version: ${{ needs.version.outputs.main_version }}
      alpha_version: ${{ needs.version.outputs.alpha_version }}
      new_version: ${{ needs.version.outputs.new_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: branch
      - name: Checkout Pages
        uses: actions/checkout@v2
        with:
          ref: pages
          path: pages

      - name: Build Alpha Pages
        run: |
          cd branch
          yarn
          yarn build
          yarn version --new_version $alpha_version --no-git-tag-version
          yarn pack
          cd $GITHUB_WORKSPACE

          rm -rf pages/$alpha_version
          mkdir -p pages/$alpha_version
          cp -rf branch/dist/* pages/$alpha_version
          cp -rf branch/*.tgz pages/$alpha_version

          cd pages
          git config --global user.name 'deploy'
          git config --global user.email 'deploy@users.noreply.github.com'
          git add .
          git commit -m "Updated Alpha $alpha_version"
          git push

      # PR 에서 처음 한번만
      - name: Comment PR
        uses: thollander/actions-comment-pull-request@v2
        env:
          main_version: ${{ needs.version.outputs.main_version }}
          alpha_version: ${{ needs.version.outputs.alpha_version }}
          new_version: ${{ needs.version.outputs.new_version }}
        with:
          reactions: rocket
          message: |
            ## Alpha ${{ github.env.alpha_version }} Build Done!

            Expected New Version: ${{ github.env.new_version }}

            ### Check it out!

            - Go to [StoryBook](https://42-world.github.io/42world-Design-System/${{ github.env.alpha_version }}/storybook)
            - Go to [Framer](https://42-world.github.io/42world-Design-System/${{ github.env.alpha_version }}/framer/index.js)

            ### Or you can try...

            ```
            yarn add @42world/design-core@https://42-world.github.io/42world-Design-System/${{ github.env.alpha_version }}/42world-design-core-v${{ github.env.main_version }}.tgz
            ```

      # 빌드 될때마다 re build done! (커밋) 이 어떤지
      - name: Comment PR
        uses: thollander/actions-comment-pull-request@v2
        env:
          main_version: ${{ needs.version.outputs.main_version }}
          alpha_version: ${{ needs.version.outputs.alpha_version }}
          new_version: ${{ needs.version.outputs.new_version }}
        with:
          reactions: rocket
          message: |
            Alpha ${{ github.env.alpha_version }} Rebuilt ${{ github.sha }}
