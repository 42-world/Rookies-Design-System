import { ControlType } from 'framer';
import type { ComponentProps } from 'react';
import { FramerProvider, applyFramerProperties } from '../../common/framer';
import { {{pascalCase name}} as _{{pascalCase name}} } from './{{pascalCase name}}';

export function {{pascalCase name}}(props: ComponentProps<typeof _{{pascalCase name}}>) {
  return (
    <FramerProvider>
      <_{{pascalCase name}} {...props} />
    </FramerProvider>
  );
}

applyFramerProperties({{pascalCase name}}, {
  // Add your custom property infos here
});
